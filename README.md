# 💼 Проект "Виджет банковских операций клиента"

## 📃 Описание:

Проект "Виджет банковских операций клиента" - это виджет на Python, который возвращает строку с замаскированным номером и счетом, а так же выводит дату последней операции с картой. Добавлены функции по работе с выполненными заявками, а так же функция сортировки по дате исполнения банковских операций. Работа над проектом еще ведется и это не финальный результат.

## 🛠️ Технологии:

Язык: Python <img src="/img/terminal.svg" alt="terminal">

Тесты: Pytest <img src="/img/beaker.svg" alt="beaker">

Виртуальное окружение: Poetry  <img src="/img/poetry.svg" alt="poetry">

Линтеры: Black <img src="/img/linter.svg" alt="linter"> 
Flake8 <img src="/img/linter.svg" alt="linter"> 
Isort <img src="/img/linter.svg" alt="linter"> 
Mypy <img src="/img/linter.svg" alt="linter">

Локальный репозиторий: Git <img src="/img/git.svg" alt="git">

Удаленный репозиторий: <img src="/img/logo_github.svg" alt="logo_github">  <img src="/img/mark_github.svg" alt="mark_github">  <img src="/img/pull_request.svg" alt="pull_request">  

## 🔧 Установка:

1. ⚙️ Клонируйте репозиторий:
```
git clone https:/nchupinin/Homework-9.1-Poetry.git
```

2. ⚙️ Установите зависимости, что бы работать с проектом:
```
1. Активировать виртуальное окружение Poetry.
Если проект уже использует Poetry, но окружение не активировано:
    
    Введите: poetry shell 
        
2. Установить зависимости.
Если зависимости ещё не установлены (например, после клонирования репозитория):
    
    Введите: poetry install 
    
3. Проверить структуру проекта.
Убедитесь, что: 
   
    * Файл pyproject.toml существует (главный конфиг Poetry).
    * Нужные скрипты/модули находятся в папке src/ или корне проекта.
    
4. Запустить проект
    
    Введите: poetry run python main.py
    
Или просто активируйте окружение и запустите проект. 
Так или иначе виджет будет выдавать результат так как у него одна точка входа. 
```
⚙️ Дополнительные команды (если нужно):

Добавить новую зависимость:

    Введите: poetry add numpy  # для основной зависимости
    Введите: poetry add pytest --group dev  # для dev-зависимости

Обновить зависимости:
    
    Введите: poetry update

Проверить список зависимостей:

    Введите: poetry show --tree

Деактивация окружения
После работы выйдите из виртуального окружения:

    Введите: exit

или закройте терминал.

## 🚀 Запуск / Использование:

1. Перейдите в файл main.py.
2. Подставьте данные которые нужны. Можете заменить данные и добавить свои.
3. Наслаждайтесь результатом.

## 📚 Модули проекта:

### masks.py
Функции для маскирования конфиденциальных финансовых данных.
* Функции:
    - **`get_mask_card_number(card_number: str) -> str`**
    Маскирует номер карты в формате XXXX XX** **** XXXX.
      - Проверки:
        - Валидация типа данных (только строка)
        - Проверка на пустые значения
        - Допустимая длина (13-19 цифр)
        - Только цифры в номере

    - **`get_mask_account(account_number: str) -> str`**
    Маскирует номер счета, оставляя видимыми последние 4 цифры (**XXXX).
      - Проверки:
        - Минимальная/максимальная длина (4-20 цифр)
        - Запрет специальных символов
        - Обработка None и пустых строк

### processing.py
Обработка и фильтрация финансовых операций.
* Функции:
    - **`filter_by_state()`**
    Фильтрует операции по статусу (по умолчанию - "EXECUTED").
      - Поддерживает:
        - Плоские списки операций
        - Вложенные структуры (кортежи списков, списки списков)
        - Автоматическое "разворачивание" вложенных коллекций

    - **`sort_by_date()`**
    Сортирует операции по дате.
      - Особенности:
        - Поддержка формата ISO 8601
        - Сортировка по убыванию (новые → старые) по умолчанию
        - Возможность реверса (аргумент reverse=False)

### widget.py
Интерфейсные функции для работы с финансовыми данными.
* Функции:
    - **`get_date(date: str) -> str`**
    Конвертирует дату из ISO-формата в ДД.ММ.ГГГГ.
      - Строгие проверки:
        - Точная длина строки (26 символов)
        - Корректность формата (YYYY-MM-DDTHH:MM:SS.ffffff)
        - Валидность чисел месяца (1-12) и дня (1-31)

    - **`mask_account_card(info: str) -> str`**
    Универсальный маскировщик для:
        - Карт (сохраняет первые 6 и последние 4 цифры)
        - Счетов (только последние 4 цифры)
        - Автоопределение типа данных по префиксу ("Счет" / платежная система)
### generators.py
Генераторы финансовых данных.
* Функции:
    - **`filter_by_currency()`**
    Итератор для фильтрации транзакций по валюте.
      - Особенности:
        - Глубокий поиск в структуре операции
        - Пропуск некорректных данных без прерывания

    - **`transaction_descriptions()`**
    Генератор описаний операций.
      - Использование:
        ```
            for desc in transaction_descriptions(transactions):
                print(desc)    
        ```    
    
    - **`card_number_generator()`**
    Генератор номеров карт в формате 0000 0000 0000 0001.
      - Параметры:
        - start - начальный номер (по умолчанию 1)
        - stop - конечный номер (необязательный)

### Архитектурные принципы
- Типизация - строгая проверка типов через аннотации 
- Обработка ошибок - единообразные исключения для невалидных данных 
- Гибкость - поддержка вложенных структур данных 
- Безопасность - полное маскирование конфиденциальных данных

Для интеграции модулей используйте импорты:

```
from src.masks import get_mask_account
from src.processing import sort_by_date
```
## 🧪 Тестирование:

В проекте реализован набор автоматических тестов, проверяющих корректность работы основных функций. Тесты написаны с использованием библиотеки `pytest`. Тесты используют параметризацию, что позволяет легко добавлять новые тестовые случаи без изменения кода тестов.

## Структура тестов

### 1. Тесты обработки данных (`test_processing.py`)
Проверяют функции фильтрации и сортировки данных:

- **`test_valid_filter_by_state`**  
  Проверяет корректность фильтрации по статусу (`state`):
  - Фильтрует только элементы с указанным статусом
  - Возвращает правильное количество элементов
  - Поддерживает вложенные структуры данных

- **`test_invalid_filter_by_state`**  
  Проверяет обработку невалидных данных:
  - Неправильные типы данных (например, строка вместо списка)
  - Отсутствие ключа `state`
  - Возвращает пустой список для некорректных входных данных

- **`test_valid_sort_by_date`**  
  Проверяет корректность данных с датами:
  - Наличие ключа `date` в каждом элементе
  - Длина строки с датой равна 26 символам
  - Поддерживает различные форматы вложенных данных

### 2. Тесты маскирования данных (`test_masks.py`)
Проверяют функции работы с номерами карт и счетов:

- **`test_get_mask_card_number`**  
  Проверяет валидность номеров карт (16 цифр)

- **`test_invalid_card_numbers`**  
  Проверяет обработку ошибок:
  - Пустые значения
  - Неправильные типы данных
  - Некорректная длина
  - Символы вместо цифр

- **`test_valid_account_numbers`**  
  Проверяет валидность номеров счетов (4-16 цифр)

- **`test_invalid_account_numbers`**  
  Аналогично тесту для карт, но с учетом особенностей счетов

- **`test_valid_get_data`**  
  Проверяет обработку дат (длина 26 символов)

- **`test_invalid_get_data`**  
  Проверяет обработку некорректных дат

### 3. Тесты виджетов (`test_widget.py`)
Проверяют функции работы с датами и маскирования данных:

- **`test_valid_get_data`**
  Проверяет корректное преобразование формата даты:
    - Конвертирует ISO-формат в формат ДД.ММ.ГГГГ
    - Обрабатывает различные корректные даты
    - Сохраняет правильный порядок дня, месяца и года

- **`test_invalid_get_data`**
    Проверяет обработку некорректных данных:
    - Неправильные разделители в дате
    - Невозможные значения месяцев (>12) и дней (>31)
    - Пустые значения и None
    - Неправильные типы данных (числа вместо строк)
    - Вызывает ValueError для невалидных входных данных

- **`test_valid_mask_account_card`**
    Проверяет корректное маскирование данных:
    - Для счетов: оставляет видимыми только последние 4 цифры
    - Для карт: сохраняет первые 6 и последние 4 цифры, маскируя остальные
    - Поддерживает различные платежные системы (Visa, МИР и др.)
    - Корректно обрабатывает номера разной длины

### 4. Тесты генераторов (test_generators.py)
Проверяют функции генерации данных и фильтрации транзакций:

- **`test_filter_by_currency`**
    Проверяет фильтрацию транзакций по валюте:
    - Возвращает только транзакции с указанной валютой
    - Корректно обрабатывает случаи отсутствия транзакций с запрошенной валютой
    - Сохраняет порядок транзакций согласно исходным данным
    - Дополнительно проверяет статус транзакций для USD

- **`test_transaction_descriptions`**
    Проверяет корректность описаний транзакций:
    - Соответствие фактических описаний ожидаемым
    - Полноту списка описаний
    - Порядок следования описаний

- **`test_valid_card_number_generator`**
    Проверяет генератор номеров карт:
    - Корректную последовательность номеров
    - Поддержку формата с 16 цифрами
    - Работу с указанием начального и конечного значений
    - Корректное увеличение номера на 1 для каждого следующего элемента

- **`test_invalid_card_number_generator`**
    Проверяет обработку некорректных входных данных:
    - Неподдерживаемые типы данных (None, строки, float)
    - Недопустимые значения (отрицательные числа)
    - Вызывает соответствующие исключения для невалидных входных данных  

## Общие особенности тестов:
- Использование параметризации для тестирования множества случаев
- Четкое разделение на валидные и невалидные сценарии
- Проверка не только основного функционала, но и граничных случаев
- Детальные сообщения об ошибках в ассертах
- Использование фикстур для подготовки тестовых данных
- Проверка типов возвращаемых значений
- Тестирование обработки ошибок и исключительных ситуаций
## Фикстуры и параметризация

Тесты используют подготовленные фикстуры и параметризацию с различными сценариями данных:

- Валидные и невалидные номера карт
- Валидные и невалидные номера счетов
- Корректные и некорректные даты
- Различные структуры данных для фильтрации
- Разнообразные примеры данных для сортировки

## Как запустить тесты

1. Убедитесь, что установлены все зависимости:
    * Проект был написан через виртуальное окружение Poetry, обычно подтягиваются все зависимости автоматически.
    * Если используете venv, то введите команду:  
      ```
      pip install -r requirements.txt
    * Так же не забывайте установить  pytest-cov, для получения более подробного вывода. И иметь возможность просматривать подробную информацию о покрытии тестов.
2. Запустите все тесты:
   * Если Venv to введите: 
      ```
      pytest -v
    
   * Если Poetry no введите:  
      ```
     pytest
     
3. Для запуска конкретного теста введите например:
     ```
     pytest -v tests/test_processing.py::test_valid_filter_by_state
4. Для получения более подробного вывода в терминале введите:
     ```
   pytest --cov=src
   
Покрытие тестов составляет: TOTAL  Stmts = 50, Miss = 6, Cover = 88%. Что укладывается в нужный показатель.

## 📊 Отчеты о тестировании:

Для просмотра детального отчёта о покрытии кода тестами:

1. Сгенерируйте HTML-отчёт:
   ```bash
   pytest --cov=main --cov-report=html
2. Откройте файл htmlcov/index.html в браузере:

    ```bash
    open htmlcov/index.html  # Для MacOS/Linux
    start htmlcov/index.html # Для Windows
   
3.  Или введите вот такую команду:
     
    ```bash
    start coverage_report/index.html


## 📊 Покрытие тестами

Последний отчёт о покрытии:  
[Открыть HTML-отчёт](coverage_report/index.html)

## 📄 Документация:

Дополнительную информацию о проекте можете узнать у меня. Можете написать мне в телеграмм @nchpnn, или позвонить по номеру телефона +7 991 631 47 09.

## 🪪 Лицензия:

Проект распространяется под [лицензией MIT](LICENSE).
